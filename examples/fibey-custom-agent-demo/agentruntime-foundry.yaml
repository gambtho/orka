# Export input for orka-acp-runtime --export-registration.
# Run inside the configured supervisor container; see README.md.
# The exporter fills the pinned identity, digests, limits, and guarantees.
apiVersion: core.orka.ai/v1alpha1
kind: AgentRuntime
metadata:
  name: fibey-foundry-runtime
spec:
  contractVersion: orka.harness.v2
  deployment:
    mode: external-endpoint
    endpoint: http://fibey-foundry-runtime.orka-system.svc.cluster.local:8080
  clientAuth:
    controllerBearerTokenSecretRef:
      name: fibey-foundry-controller-auth
      key: token
    operationCapabilitySecretRef:
      name: fibey-foundry-operation-auth
      key: capability-secret
  capabilities:
    profile:
      providerKind: foundry
      workspaceIntent: read
    mcpPolicy:
      allowedTools: []
      disallowedTools: []
      allowBash: false
      approvalRequiredTools: []
